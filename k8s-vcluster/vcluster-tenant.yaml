controlPlane:
  statefulSet:
    persistence:
      volumeClaim:
        enabled: false
    scheduling:
      podManagementPolicy: OrderedReady
  serviceMonitor:
    enabled: true

policies:
  resourceQuota:
    enabled: true
    quota:
      requests.cpu: "4"
      requests.memory: 16Gi
      limits.cpu: "8"
      limits.memory: 32Gi