controlPlane:
  statefulSet:
    persistence:
      volumeClaim:
        enabled: false
    scheduling:
      podManagementPolicy: OrderedReady
  serviceMonitor:
    enabled: true

# policies:
#   resourceQuota:
#     enabled: true
#     quota:
#       cpu: "10"
#       memory: 20Gi
#       pods: "10"
